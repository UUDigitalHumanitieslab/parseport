import { buildTime, version, sourceUrl } from "./version";

export const environment = {
    production: true,
    assets: "/static/assets",
    apiUrl: "/api/spindle",
    buildTime,
    version,
    sourceUrl,
};
